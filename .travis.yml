language: python
install: pip install tox
matrix:
  include:
    - python: 2.7
      env:
       - TOX_ENV=py27
    - python: 3.6
      env:
        - TOX_ENV=py36
    - python: 3.7
      env:
        - TOX_ENV=py37
script:
  - flake8 src/h3 setup.py tests
  - tox -e $TOX_ENV
after_success:
  - codecov
