language: python
cache: pip
python:
  - 2.7
  - 3.6
  - 3.7
install:
  - pip install -r requirements-dev.txt
  - pip install .
script:
  - flake8 src/h3 setup.py tests
  - pytest tests/* --cov=h3
after_success:
  - codecov
